# 安装 aidisc

## 方式 A：直接加载完整源码（推荐）

1. 下载发行包：仓库中的发布或本地 `aidisc-v1.2.3.zip`
2. 解压后在 `chrome://extensions` 开启开发者模式，「加载已解压的扩展程序」

## 方式 B：从本仓库 clone

```bash
git clone https://github.com/zmt18928708528-eng/aidisc.git
cd aidisc
bash scripts/extract-core.sh   # 解压核心 JS/图标
```

然后用 Chrome 加载本目录。

仓库地址：https://github.com/zmt18928708528-eng/aidisc
